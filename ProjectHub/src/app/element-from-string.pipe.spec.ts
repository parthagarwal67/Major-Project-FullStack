import { ElementFromStringPipe } from './element-from-string.pipe';

describe('ElementFromStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementFromStringPipe();
    expect(pipe).toBeTruthy();
  });
});
